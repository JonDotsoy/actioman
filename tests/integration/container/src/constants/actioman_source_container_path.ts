/**
 * Static variable representing the Actioman source directory path inside the container.
 * @type {URL}
 */
export const actiomanSourceContainerPath = new URL(
  "file:///usr/share/actioman/",
);
